#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/bash" ]; then
  "$basedir/bash"  "$basedir/../../../_gorillascript@0.9.10@gorillascript/bin/gorilla" "$@"
  ret=$?
else 
  bash  "$basedir/../../../_gorillascript@0.9.10@gorillascript/bin/gorilla" "$@"
  ret=$?
fi
exit $ret
