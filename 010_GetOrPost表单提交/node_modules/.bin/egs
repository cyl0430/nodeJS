#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/bash" ]; then
  "$basedir/bash"  "$basedir/../_egs@0.3.1@egs/bin/egs" "$@"
  ret=$?
else 
  bash  "$basedir/../_egs@0.3.1@egs/bin/egs" "$@"
  ret=$?
fi
exit $ret
